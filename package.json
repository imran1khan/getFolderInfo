{
  "name": "getjsonfromfolder",
  "version": "1.0.2",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "prestart": "tsc -b",
    "start": "node dist/app.js",
    "serve": "tsc -w & nodemon dist/app.js",
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "nodemon": "^3.1.0"
  }
}
